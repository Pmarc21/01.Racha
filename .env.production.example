# Dominio (sin https://)
DOMAIN=racha.tudominio.com

# Base de datos
POSTGRES_DB=racha
POSTGRES_USER=racha
POSTGRES_PASSWORD=cambia_esto_por_algo_seguro

# JWT secret (genera uno con: openssl rand -hex 32)
SECRET_KEY=cambia_esto_por_algo_seguro

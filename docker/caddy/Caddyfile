{$DOMAIN} {
    # API y auth → backend
    handle /api/* {
        reverse_proxy api:8000
    }
    handle /auth/* {
        reverse_proxy api:8000
    }
    handle /users/* {
        reverse_proxy api:8000
    }

    # Todo lo demás → frontend
    handle {
        reverse_proxy frontend:80
    }
}
